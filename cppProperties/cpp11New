nullptr是C++11的新特性，以前空指针也是用NULL，也就是0，但是这样使用的话，如果有重载，就分不清了。使用nullptr可以分开空指针和0。


auto: 类型推导
经常使用于迭代器中，以及变量定义
但是有一点不能用，就是函数传参，不能使用auto。
void fun(auto a) 像这样的就不能用，编译器会直接报错
数组类型也不行   auto a[10]= x； 不能这样使用。

区间迭代
for的另类使用
for(auto &i: arr);


